Pick the best Rate_Master_Output candidate for each extracted BOQ product.

Return valid JSON only. Prefer null over a weak guess. Never invent Product_ID, prices, or vendors.

{{SYNONYM_MAP}}

Section vs product:
- ``extracted`` fields (description_hint, category, sub_category, class, size, unit,
  capacity, attributes) define the product to match — especially on rematch when the
  expert edited them.
- ``description_hint`` is the AI/expert understanding of the product. On rematch it may
  be free-form correction (e.g. "wrong — find sand buckets in db"). Interpret the
  intended product from that text; do not treat instructional words as catalog labels.
- ``boq_row.description`` is the FULL section for meaning only (system title + notes).
- ``boq_row.slot_description`` is the qty/unit line for that product.
- Do NOT pick Hydrant / Landing Valve / etc. only because the section title says
  hydrant when the product fields/slot describe pipe, valve, or another item.

Rules:
- ``selected_id`` = a candidate ``id`` (Rate_Master_Output PK) or null.
- Candidates are best-first. On a true tie, pick the first.
- Prefer: size digits → category/sub_category → class/capacity/attributes.
- Wrong size or wrong product family → null.
- Class ``0`` is a real valve class. PIPE MS/GI live in sub_category. Ignore make/vendor.
- Rematch (``extracted.rematch`` true): trust ``expert_filled_fields`` +
  ``expert_filled_attributes`` + slot/section context; keep ``prior_match`` only if
  it is still the best among candidates. When ``description_hint`` names a different
  product than prior taxonomy, follow the description.
- Map attributes onto candidate schema; leftovers in ``unmapped_attributes``.
- ``match_confidence`` 0–100 (0 if null; &lt;30 if uncertain).

Respond with:
{
  "products": [
    {
      "product_ref": "p0",
      "selected_id": 73,
      "match_confidence": 78,
      "attribute_map": {"port_type": "type"},
      "mapped_attributes": {"type": "full port"},
      "unmapped_attributes": {},
      "notes": "Size and sub-category align."
    }
  ]
}

PRODUCTS_PAYLOAD:
{{PRODUCTS_PAYLOAD}}
