Pick the best Rate_Master_Output candidate for each extracted BOQ product.

Return valid JSON only. Prefer null over a weak guess. Never invent Product_ID, prices, or vendors.

{{SYNONYM_RULES}}

Product family first — always:
- Match the product FAMILY (category/sub_category) before anything else.
- A candidate is ONLY eligible if its category AND sub_category both match the extracted product.
  Wrong family → selected_id: null, even if size or other fields match perfectly.
  Examples: sluice valve vs PIPE; pipework vs SPRINKLER head; hose reel vs hose box.
- **CRITICAL**: If the extracted product's Category or Sub_Category is null, empty, or "Not found", you MUST penalize the match. The maximum allowed `match_confidence` for such items is 45%, regardless of how well the description hint matches the candidate. Never output a high confidence score if the true category was unconfirmed.
- Then within the correct family: prefer the candidate whose size, class, capacity, and
  attributes best match the extracted fields.
- Class ``0`` is a real valve class. PIPE MS/GI live in sub_category. Ignore make/vendor.

Priority order for matching:
1. Category match (required — eliminates candidates)
2. Sub_category match (required — eliminates candidates)
3. Size / nominal bore match (high weight)
4. Class match (medium weight)
5. Capacity / attribute match (lower weight)

Key rules:
- ``extracted.description_hint`` (AI Description) is the ABSOLUTE HIGHEST priority for
  understanding what product was bought. If it says "landing valve", find a landing valve.
- ``extracted.category`` + ``extracted.sub_category`` carry the most weight after the hint.
- ``boq_row.description`` / ``slot_description`` are section context only — they validate
  meaning but do not override the extracted sub_category.
- Do NOT assume an item is a pipe just because it has a diameter in mm. Focus on the core
  product noun (e.g. vibration eliminator, landing valve, branch pipe, sprinkler head).
- Rematch (``extracted.rematch`` true): use expert-filled inputs as hints, validate against
  BOQ meaning. Confidence = how well the DB row matches extracted identity.
- Map attributes onto candidate schema; leftovers in ``unmapped_attributes``.
- ``match_confidence`` 0–100 (0 if null; <30 if uncertain). Calculate this score strictly based on how well the candidate's fields (Category, Sub_Category, Size, Class, Capacity) match the `extracted.description_hint`. Do NOT assign random or arbitrary scores. If size, material, or sub-category mismatches, the score MUST drop significantly. Industry abbreviations that mean the same thing must not lower confidence.
- Candidates are best-first. On a true tie, pick the first.

Respond with:
{
  "products": [
    {
      "product_ref": "p0",
      "selected_id": 73,
      "match_confidence": 78,
      "attribute_map": {"port_type": "type"},
      "mapped_attributes": {"type": "full port"},
      "unmapped_attributes": {},
      "notes": "Sub-category and size align."
    }
  ]
}

PRODUCTS_PAYLOAD:
{{PRODUCTS_PAYLOAD}}
