Pick the best Rate_Master_Output candidate for each extracted BOQ product.

Return valid JSON only. Prefer null over a weak guess. Never invent Product_ID, prices, or vendors.

{{SYNONYM_RULES}}

Section vs product:
- ``extracted.description_hint`` (AI Description) is the primary search identity —
  it should already name Category / Sub_Category / Class / Size.
- ``extracted.category`` + ``extracted.sub_category`` carry the most weight after
  the description (Sub_Category ≈ product name). Then class, size, unit, capacity.
- ``boq_row.description`` / ``slot_description`` are section evidence to validate
  meaning (pipework ≠ sprinkler head; sluice valve ≠ PIPE).
- Expert fields are hints; if they contradict the BOQ purchasable item, ignore them.

Rules:
- ``selected_id`` = a candidate ``id`` (Rate_Master_Output PK) or null.
- Candidates are best-first. On a true tie, pick the first.
- Choose the candidate that satisfies product family first (category/sub), then
  nominal size, class/capacity, attributes — not the one with the most token overlap.
- Wrong product family → null even if size matches (sluice valve vs PIPE;
  pipework vs SPRINKLER head; hose reel vs hose box).
- Class ``0`` is a real valve class. PIPE MS/GI live in sub_category. Ignore make/vendor.
- Rematch (``extracted.rematch`` true): use expert inputs as hints, but validate
  against BOQ meaning. Confidence = how well the DB row matches extract identity.
- Map attributes onto candidate schema; leftovers in ``unmapped_attributes``.
- ``match_confidence`` 0–100 (0 if null; &lt;30 if uncertain). Abbreviations /
  industry wording that mean the same thing must not lower confidence.

Respond with:
{
  "products": [
    {
      "product_ref": "p0",
      "selected_id": 73,
      "match_confidence": 78,
      "attribute_map": {"port_type": "type"},
      "mapped_attributes": {"type": "full port"},
      "unmapped_attributes": {},
      "notes": "Sub-category and size align."
    }
  ]
}

PRODUCTS_PAYLOAD:
{{PRODUCTS_PAYLOAD}}
