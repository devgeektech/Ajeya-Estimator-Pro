# Generated manually on 2026-07-08

from django.db import migrations


class Migration(migrations.Migration):

    dependencies = [
        ("costing", "0001_initial"),
        ("matching", "0002_repair_productmatch_columns"),
    ]

    operations = [
        migrations.RunSQL(
            sql="""
            CREATE TABLE IF NOT EXISTS costing_ratedetail (
                id bigint GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
                labour_master_id bigint NULL,
                tech_key varchar(255) NOT NULL DEFAULT '',
                make varchar(150) NOT NULL DEFAULT '',
                supplier varchar(150) NOT NULL DEFAULT '',
                base_purchase_rate numeric(14, 2) NOT NULL DEFAULT 0,
                discount_percent numeric(6, 2) NOT NULL DEFAULT 0,
                net_material_rate numeric(14, 2) NOT NULL DEFAULT 0,
                commercial_material_base numeric(14, 2) NOT NULL DEFAULT 0,
                accessories_value numeric(14, 2) NOT NULL DEFAULT 0,
                handling_value numeric(14, 2) NOT NULL DEFAULT 0,
                wastage_value numeric(14, 2) NOT NULL DEFAULT 0,
                subtotal_before_profit numeric(14, 2) NOT NULL DEFAULT 0,
                profit_value numeric(14, 2) NOT NULL DEFAULT 0,
                final_expenditure numeric(14, 2) NOT NULL DEFAULT 0,
                final_amount_excl_gst numeric(14, 2) NOT NULL DEFAULT 0,
                margin_percent_on_selling numeric(6, 2) NOT NULL DEFAULT 0,
                labour_type varchar(100) NOT NULL DEFAULT '',
                labour_state varchar(100) NOT NULL DEFAULT '',
                labour_rate_per_unit numeric(14, 2) NOT NULL DEFAULT 0,
                total_labour_per_unit numeric(14, 2) NOT NULL DEFAULT 0,
                total_labour_with_multiplier numeric(14, 2) NOT NULL DEFAULT 0,
                rate_contribution numeric(14, 2) NOT NULL DEFAULT 0,
                product_match_id bigint NOT NULL
            );

            CREATE UNIQUE INDEX IF NOT EXISTS costing_ratedetail_product_match_id_key
            ON costing_ratedetail (product_match_id);

            CREATE INDEX IF NOT EXISTS costing_ratedetail_labour_master_id_idx
            ON costing_ratedetail (labour_master_id);

            CREATE INDEX IF NOT EXISTS costing_ratedetail_tech_key_idx
            ON costing_ratedetail (tech_key);

            DO $$
            BEGIN
                IF NOT EXISTS (
                    SELECT 1
                    FROM pg_constraint
                    WHERE conname = 'costing_ratedetail_product_match_id_fk'
                ) THEN
                    ALTER TABLE costing_ratedetail
                    ADD CONSTRAINT costing_ratedetail_product_match_id_fk
                    FOREIGN KEY (product_match_id)
                    REFERENCES matching_productmatch(id)
                    DEFERRABLE INITIALLY DEFERRED;
                END IF;
            END
            $$;
            """,
            reverse_sql=migrations.RunSQL.noop,
        ),
    ]
